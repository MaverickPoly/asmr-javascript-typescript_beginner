const car: {model: string, type: string, color: string} = {
    model: "Camaro",
    type: "Chevrolet",
    color: "black"
}

const book: object = {
    title: "Atomic Habits",
    author: "James Clear",
    year: 2018,
}

console.log(car);
console.log(book);

const dog: {color: string, name?: string} = {
    color: "brown",
}

console.log(dog);
